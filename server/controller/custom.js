const { success } = require('./base');


// 统计报名信息列表
exports.customList = async function(ctx, next) {
  success(ctx, next, '支不支持');
}

// 统计报名信息添加
exports.customAdd = async function(ctx, next) {
  success(ctx, next, '支不支持');
}

// 统计报名信息
exports.customCount = async function(ctx, next) {
  success(ctx, next, '支不支持');
}


